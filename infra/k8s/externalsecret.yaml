apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ms-production-secrets
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: ms-production-secrets
    creationPolicy: Owner
  data:
    - secretKey: POSTGRES_DB_URL
      remoteRef:
        key: /ms-production/postgresql/uri
